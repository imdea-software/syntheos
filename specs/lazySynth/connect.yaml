property: "G(
              [a<100] & [a>b-b-frac]
            )"
variables:
  - name: a
    type: Int
    owner: system
  - name: b
    type: Int
    owner: environment
  - name: frac
    type: Int
    owner: environment

#[a>b-b*frac]