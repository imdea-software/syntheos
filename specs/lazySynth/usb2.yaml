property: "G(
              ([c==0] -> [a>100])
              & ([c==1] -> (![b==0] & [b==a*a]))
            )"
variables:
  - name: a
    type: Int
    owner: system
  - name: b
    type: Int
    owner: system
  - name: c
    type: Int
    owner: environment

